import React,{useState}from'react';export default function PromptForm({onGenerate,loading}){const[text,setText]=useState('Título: O Mesmo Céu\nGênero: Folk espiritual brasileiro / MPB contemplativo');function submit(e){e.preventDefault();if(!text.trim())return;onGenerate(text);}return(<form onSubmit={submit} className='space-y-3'><label className='block text-sm font-medium'>Prompt</label><textarea value={text} onChange={e=>setText(e.target.value)} rows={8} className='w-full p-3 border rounded resize-none text-sm'/><button className='px-4 py-2 bg-slate-800 text-white rounded disabled:opacity-50' disabled={loading} type='submit'>{loading?'Gerando...':'Gerar Música'}</button></form>);}